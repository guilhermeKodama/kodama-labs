/**
 * The canonical Kodama Labs mark used in the header, footer, hero pill, and
 * background watermark. We re-export the chosen variant from `marks.tsx` so
 * swapping the brand later means changing one import line, not chasing
 * call-sites.
 */
export { CompassMark as LogoMark } from './marks';
